<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eila.farmaciasapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />


    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_farmacia"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".Inicio"
            android:label="Inicio"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".SelecionarFarmacia"
            android:label="@string/title_activity_selecionar_farmacia"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProductosActivity"
            android:label="@string/title_activity_productos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CestaActivity"
            android:label="@string/title_activity_cesta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PagoActivity"
            android:label="@string/title_activity_pago"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PerfilActvity"
            android:label="@string/title_activity_perfil_actvity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PedidosActivity"
            android:label="@string/title_activity_pedidos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PreguntasActivity"
            android:label="@string/title_activity_preguntas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LegalActivity"
            android:label="@string/title_activity_legal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".InformacinFarmaciasActivity"></activity>
    </application>

</manifest>